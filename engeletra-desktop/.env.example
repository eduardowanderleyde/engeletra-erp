# Copie este arquivo para .env e preencha os valores antes de rodar via Docker.
#
#   cp .env.example .env
#
# Gere ENGELETRA_SECRET_KEY com:
#   python3 -c "import secrets; print(secrets.token_hex(32))"

ENGELETRA_SECRET_KEY=troque_por_uma_chave_gerada_com_secrets_token_hex_32
ENGELETRA_ADMIN_PASSWORD=troque_por_uma_senha_forte
